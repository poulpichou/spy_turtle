const tabs=[...document.querySelectorAll(".center-tab")];const views=[...document.querySelectorAll(".center-view")];const logElement=document.getElementById("robot-log");const photoPreview=document.getElementById("photo-preview");const photoEmpty=document.getElementById("photo-empty");const photoName=document.getElementById("photo-name");let logTimer=null;let photos=[];let photoIndex=0;function setCenterView(name){tabs.forEach(tab=>tab.classList.toggle("active",tab.dataset.view===name));views.forEach(view=>view.classList.toggle("active",view.id===`${name}-view`));if(name==="camera"){stopLogs();startCameraRefresh()}else{stopCameraRefresh();if(name==="logs")startLogs();else stopLogs();if(name==="photos")loadPhotos()}}tabs.forEach(tab=>tab.onclick=()=>setCenterView(tab.dataset.view));async function refreshLogs(){try{const response=await fetch("/logs?count=80",{cache:"no-store"});if(!response.ok)throw new Error(`HTTP ${response.status}`);const data=await response.json();logElement.textContent=data.lines.join("\n")||"No logs yet";logElement.scrollTop=logElement.scrollHeight}catch(error){logElement.textContent=`Unable to load logs: ${error.message}`}}function startLogs(){refreshLogs();if(!logTimer)logTimer=setInterval(refreshLogs,1000)}function stopLogs(){if(!logTimer)return;clearInterval(logTimer);logTimer=null}async function loadPhotos(){try{const response=await fetch("/photos",{cache:"no-store"});if(!response.ok)throw new Error(`HTTP ${response.status}`);const data=await response.json();photos=data.photos||[];photoIndex=Math.min(photoIndex,Math.max(0,photos.length-1));showPhoto()}catch(error){photos=[];showPhoto();photoEmpty.textContent=`Unable to load photos: ${error.message}`}}function showPhoto(){const photo=photos[photoIndex];photoPreview.style.display=photo?"block":"none";photoEmpty.style.display=photo?"none":"block";photoName.textContent=photo?`${photoIndex+1}/${photos.length} · ${photo.name}`:"";if(photo)photoPreview.src=`${photo.url}?t=${Date.now()}`}document.getElementById("photo-previous").onclick=()=>{if(!photos.length)return;photoIndex=(photoIndex-1+photos.length)%photos.length;showPhoto()};document.getElementById("photo-next").onclick=()=>{if(!photos.length)return;photoIndex=(photoIndex+1)%photos.length;showPhoto()};document.getElementById("photo-button").onclick=async()=>{try{const response=await fetch("/photos/capture",{method:"POST"});if(!response.ok)throw new Error(`HTTP ${response.status}`);await loadPhotos();photoIndex=0;showPhoto();setCenterView("photos")}catch(error){showCommandError(error)}};